# Procfile — reference only. Railway with Dockerfile ignores this.
# Set these in Railway: Service → Settings → Custom Start Command
#
# web:  node server.js
# bot:  BOT_HEALTH_PORT=3001 node bot/bot.js
